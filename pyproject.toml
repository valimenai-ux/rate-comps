[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.mypy]
python_version = "3.9"
files = ["src", "run.py", "RateComps.py"]
strict = true
warn_unreachable = true

[[tool.mypy.overrides]]
module = ["plotly.*"]
ignore_missing_imports = true
